name: Python application

on:
  push:
    branches:
      - "main"
    paths:
      - "API/main.py"
  pull_request:
    branches:
      - "main"
    paths:
      - "API/main.py"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir flake8 pytest fastapi
        if [ -f API/requirements.txt ]; then pip install --no-cache-dir -r API/requirements.txt; fi
        pip list  # Vérification des paquets installés

    - name: Ensure API package is recognized
      run: |
        touch API/__init__.py

    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Test with pytest
      run: |
        python -m pytest API/testapi.py
